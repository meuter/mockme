1. make it work with double description macros
2. make sure that mockme works with function with "..." stdarg like fprintf
3. add some new kind of target to sconscript say e.g. DoubleModule() that calls mockme properly
